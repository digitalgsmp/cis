## CARD 2.2 — PASS 2: reasoning that implies a task (SEMANTIC)

```
This is the pass that matters most and the one that was skipped.

You are not looking for someone asking for something. You are looking for
EXCHANGES where a problem was reasoned through — Eric and a model, or two
models reviewing each other — where the reasoning implies work even though
nobody wrote a task.

Eric, 2026-08-27: "my statements only maybe part of the problem. Solutions came
from the back and forth between me and a model or between models reviewing each
other... it's not just what I said, it's the clarity that is defined through
the process."

Query for the SHAPE of that reasoning:
  we discovered the real cause was
  this turned out to be different from what we thought
  the reason this fails is
  that would break because
  we should handle this differently
  this design has a flaw
  the right approach would be
  that assumption was wrong
  this only works because
  the underlying problem is
  a model reviewing another model's proposal and objecting
  a correction to a previous conclusion

found_by='P2-reasoning'. n_results=50 per query.

PROOF: count of new rows, every query with hit count, and 5 full excerpts —
I want to read whether these are actually exchanges and not single statements.
```
