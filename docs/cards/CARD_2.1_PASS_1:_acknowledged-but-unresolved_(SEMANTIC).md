## CARD 2.1 — PASS 1: acknowledged-but-unresolved (SEMANTIC)

```
Your original question: what is in the KB that acknowledges an issue to be
addressed?

Run semantically, many queries, n_results=50 each. Insert every hit as a
PENDING row with found_by='P1-acknowledged'.

Query the CONCEPTS, not the words. Examples — extend this list, and record
every query you actually ran:
  a problem that was noticed but not fixed
  something deferred until later
  a limitation accepted for now
  work that was started and not finished
  a decision postponed
  something that should be revisited
  a known gap in the system
  a workaround standing in for a real fix
  functionality described but never built
  a capability that was planned
  an issue raised during development

PROOF: paste the count of new rows, the list of every query you ran with its
hit count, and 5 random excerpts in full.
EXPECT: hundreds. If a query returns nothing useful, say so — that is data.
```
