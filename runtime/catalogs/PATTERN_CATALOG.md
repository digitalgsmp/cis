# Pattern Catalog

Generated: 2026-08-28T00:02:01.151646+00:00

Ground truth re-checked live just now (read-only): stat on all six pitfalls.md returned inodes 13781319 / 13781391 / 13781541 / 13781616 / 13781463 / 13781687, all links=1 — matching the spec's evidence table exactly. Pointer line grep -c returns 1 for all six SKILL.md files. RESULTS/ exists and holds the cited sibling artifacts (build_proof_20260703.txt, standing_container_block_proof.txt, run_v6/v7 logs). The directive is verifiable as written. Pattern catalog follows.

PATTERN CATALOG — post-rebuild pitfalls.md separation verification (zero-build)

1. LANGUAGE AND TOOLCHAIN
Verification is read-only POSIX shell (bash): test -f, stat -c '%i %h %n', find -type l, md5sum, grep -c, mount. No new runtime, no Python execution for the checks — the Python/bash pipeline substrate (tools/pipeline/*, pipeline_dispatch.sh) is not touched by this run. Deliverable is a Markdown report. Zero-build: no compiler, no dependency install, no code; the only write in the entire task is the report file itself.

2. ARCHITECTURAL PATTERNS
Zero-build verification: the spec freezes already-existing read-only commands as a reproducible protocol and makes the evidence permanent; nothing gets constructed. Report-as-proof-artifact: the report lands in enforcement/mwl-proof-v2/RESULTS/ next to existing container proof artifacts — proof lives with enforcement evidence, not in repo root or docs. Evidence-first reporting: every claim in the report must be a verbatim transcript of fresh tool output; the report may not copy the spec's numbers, it must re-run and paste (self-report is not truth). Fail-don't-fix: a failed check is reported as FAIL with raw evidence, never repaired — verification is observational, not corrective. Nuance-vs-failure: intentional design properties (review1/menter/verify byte-identical content) get a NUANCE section with rationale, not a FAIL — grounded in skill-split-per-profile.md's stated purpose of independent write targets. Master-mirror pairing: live homes at /home/worker/.hermes-<role>/ compared by md5 against repo masters at enforcement/mwl-proof-v2/cis-skills-per-role/<role>/; md5 equality is the drift detector.

3. CONVENTION RULES
Fixed exact artifact path, snake_case with date stamp: pitfalls_separation_post_rebuild_verification_20260827.md — no variant names or version suffixes. Read-only discipline: every command except the report write is read-only; no touch/chmod/sed or any write into profile homes or repo masters; the mtime guard (no live-file mtime newer than rebuild time) is what proves read-only. Verbatim quoting: md5s, inode tables, and transcripts pasted raw — paraphrased numbers are a pattern failure. Fixed section order a-f: header, evidence table, per-check results, nuance statement, residual risks, verdict line. Fixed verdict format: "SPLIT SURVIVED REBUILD: PASS" or FAIL. Stable check labels CHK-1..CHK-6 so the Verifier checklist can reference them. No silent skips: every check yields PASS or FAIL with evidence.

4. TEST CONVENTION
Six standalone shell checks, each a single pass/fail with quoted output. CHK-1 presence: test -f per role, all six true. CHK-2 no-sharing: stat %i %h per role — six distinct inodes, all links=1 — plus find -type l returning empty. CHK-3 drift: md5 of each live pitfalls.md versus its per-role repo master, all six identical. CHK-4 SKILL.md: md5 versus per-role master, all six identical (this also disproves reseeding from the stale 437-line copy). CHK-5 pointer: grep -c "See references/pitfalls.md" >= 1 per role (confirmed live: 1 each). CHK-6 mounts: mount | grep '/home/worker/.hermes' yields no output. Overall PASS requires all six; any single failure makes the run FAIL. Independent re-verification: the Verifier re-runs CHK-2 and CHK-3 and the numbers must match the evidence table exactly. Live-tree checks only — never seed or archive copies. No test framework, no fixtures, no mocks.

5. COMPLETION CRITERIA PER PATTERN (what done looks like)
Presence: done = six true verdicts quoted verbatim; one missing tuple = not done. No-sharing: done = six stat tuples quoted, each distinct inode with links=1, plus quoted empty symlink output; an unquoted claim = not done. Drift: done = six md5 pairs quoted; any mismatch = FAIL with the mismatch quoted. SKILL.md: done = six md5s quoted, matching per-role masters — simultaneously the proof the stale copy was not re-seeded. Pointer: done = six grep -c outputs, each >= 1. Mounts: done = the empty output itself quoted or explicitly stated absent. Report: done = file exists at the exact path, all six sections a-f present in order, verdict line present and consistent with the evidence table. Ground-truth snapshot: my fresh stat run matches the spec's inode table exactly (13781319, 13781391, 13781541, 13781616, 13781463, 13781687, all links=1), so the spec is verifiable as written.

6. FILES MENTER WILL CREATE OR MODIFY
Create exactly one file: /workspace/cis/enforcement/mwl-proof-v2/RESULTS/pitfalls_separation_post_rebuild_verification_20260827.md — nothing else. Modify: none. Zero changes to profile homes, repo masters, pipeline code, or config.

Bias check: this directive is clean. No committees, no approval chains, no multi-tenant, no new infrastructure — a single observational report. The only residual risk (R1: future rebuilds could re-seed from the stale pre-split copy if run_container.sh seeding changes) is correctly documented as watch-only, not built into new machinery.

{"role":"brain","status":"READY","summary":"Zero-build read-only verification pattern catalog. Toolchain: read-only POSIX shell (stat, md5sum, grep -c, find, mount) over the existing Python/bash pipeline substrate; deliverable is one Markdown report. Architecture: report-as-proof-artifact (lands in enforcement/mwl-proof-v2/RESULTS/ beside existing build/container proofs), evidence-first verbatim quoting, fail-don't-fix, nuance-vs-failure for the intentional review1/menter/verify content convergence. Conventions: fixed snake_case date-stamped path, fixed section order a-f, fixed verdict line format, CHK-1..CHK-6 labels. Tests: six shell checks, all must PASS, Verifier independently re-runs CHK-2/CHK-3. Completion: six quoted stat tuples with distinct inodes and links=1, six md5 pairs identical to per-role masters, pointer line count >=1 per role, empty mount output, report file with all six sections and consistent verdict. Ground truth re-verified live: inodes 13781319/13781391/13781541/13781616/13781463/13781687, all links=1, pointer count 1 per role — spec is verifiable as written. Single file to create; nothing to modify.","files_planned":["/workspace/cis/enforcement/mwl-proof-v2/RESULTS/pitfalls_separation_post_rebuild_verification_20260827.md"]}