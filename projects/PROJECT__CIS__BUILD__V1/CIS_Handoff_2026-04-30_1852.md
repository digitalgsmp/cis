# CIS Session Handoff
## Date: 2026-04-30
## Time: 1852 UTC

## Claude's Duty at Session Start

At the start of every session Claude must:

1. Read the handoff file and 2_CIS_REORIENTATION.md
2. Confirm the current build target from Next Steps
3. Instruct the human to open a CIS Live session before any build work begins:
   - Topic = current build target
   - Problem = where we are, what we are about to build, and the expected
     return point to the main path if this is a branch
4. Keep that Live session referenced throughout — log unexpected insights
   or architectural decisions as rounds
5. Remind the human to resolve the Live session before closing

Claude must not begin build work until the Live session is open.

---

## Claude's Duty at Session End

At the end of every session — when the human signals the session is ending —
Claude must:

1. Confirm the CIS Live session has been resolved (or note it as open in Notes)
2. Generate the four session close fields for pasting into the dashboard

Claude generates these fields once, at the exact moment the session ends.
If additional work happens after fields are generated, Claude must say:
"Those fields are now outdated — here are the updated ones:" and regenerate
before anything is pasted.

Field 1 — Session Focus: One sentence. The primary objective of this session.
Field 2 — Completed: Specific past-tense list of what was built, fixed, or decided.
Field 3 — Next Steps: Prioritized list of what the next session does first.
Field 4 — Notes: Decisions discussed but not locked. Warnings or gotchas. Mid-thought or
partially built work. Anything deferred that is not a next step. Omit if nothing qualifies.

---

## Session Focus
Draft and lock CIS_Automation_Reduction_Contract_v1.md (ADR-046) and implement ADR-045 Steps 1–4 — execution queue ownership layer operational with verify_contract running end-to-end through the worker.

## Completed
CIS_Automation_Reduction_Contract_v1.md drafted, audited twice by ChatGPT (PASS both), locked as ADR-046
ADR-045 Step 1: execution_jobs table created via migration script, cis_verify_jobs.py built, PASS 17/17
ADR-045 Step 2: api/queue.py built with enqueue/status/job/cancel routes, cis_test_queue.py built, PASS 9/9
ADR-045 Step 3: Flask-started background worker built in queue_worker.py, cold-start recovery included, thread_alive confirmed
ADR-045 Step 4: verify_contract operator route refactored to enqueue, worker executes cis_verify.py, PASS 21/21
app.py updated with queue_bp and worker startup
4 session manifests written to /mnt/projects/cis/logs/manifests/

## Next Steps
ADR-045 Step 5: dashboard polling — auto-refresh worker-status and job status on existing surface, no UI expansion
ADR-045 Step 6: run_l2 job type end-to-end verification
ADR-045 Step 7: cold start recovery test
Canonical queue_worker.py — current VM version was patched in-place; cat file and confirm project copy matches before next session

## Notes
queue_worker.py patched in-place via terminal commands — download version may not match VM; verify at next session start
Downloads folder file-overwrite issue: browser renames to filename(1) if old version exists — clear Downloads before each download; will be eliminated as automation matures
ADR list sort order (newest-first) and sticky header flagged as UI debt — fix when next touching dashboard
Step 5 touches cis_dashboard.html monolith — surgical addition only per PD.5 contract, no expansion

## Sync Status
✓ Session logged to database
✓ ADRs.md regenerated (47 decisions)
✓ Knowledge records synced
✓ Projects folder synced
✓ Runtime scripts synced to vault
✓ system_log.md updated

## How to start next session
Run on Ubuntu VM:
```
cis-start
```
Paste output as first message in new chat, then attach this file.
