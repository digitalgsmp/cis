# CIS Session Handoff
## Date: 2026-04-24
## Time: 0530 UTC

## Claude's Duty at Session Start

At the start of every session Claude must:

1. Read the handoff file and 2_CIS_REORIENTATION.md
2. Confirm the current build target from Next Steps
3. Instruct the human to open a CIS Live session before any build work begins:
   - Topic = current build target
   - Problem = where we are, what we are about to build, and the expected
     return point to the main path if this is a branch
4. Keep that Live session referenced throughout — log unexpected insights
   or architectural decisions as rounds
5. Remind the human to resolve the Live session before closing

Claude must not begin build work until the Live session is open.

---

## Claude's Duty at Session End

At the end of every session — when the human signals the session is ending —
Claude must:

1. Confirm the CIS Live session has been resolved (or note it as open in Notes)
2. Generate the four session close fields for pasting into the dashboard

Claude generates these fields once, at the exact moment the session ends.
If additional work happens after fields are generated, Claude must say:
"Those fields are now outdated — here are the updated ones:" and regenerate
before anything is pasted.

Field 1 — Session Focus: One sentence. The primary objective of this session.
Field 2 — Completed: Specific past-tense list of what was built, fixed, or decided.
Field 3 — Next Steps: Prioritized list of what the next session does first.
Field 4 — Notes: Decisions discussed but not locked. Warnings or gotchas. Mid-thought or
partially built work. Anything deferred that is not a next step. Omit if nothing qualifies.

---

## Session Focus
Session Focus Phase 1 open — defined Segment and knowledge_spine as canonical objects, locked domain taxonomy, and wrote all Phase 1 DB migrations.

## Completed
Written and logged ADR-031: Segment as canonical object, scene detection required, fixed interval rejected
Written and logged ADR-032: LIFE and CREATION as top-level CIS domains, knowledge_spine as first-class object
Designed spine ingestion pipeline — 5-step sequence from anchor document to approved spine
Written knowledge_spine_v1.json and spine_node_v1.json schema files to /mnt/projects/cis/runtime/schemas/
Updated knowledge_v1.json schema with anchor_node_id field
Created DB migration script and ran Phase 1 migrations — knowledge_spines, spine_nodes, segments tables created
Added ensure_phase1_tables() to connection.py and wired into system.py startup
Clarified dual storage architecture: SQLite = operational state, JSON/MD files = knowledge record content
Identified that knowledge_records has no DB table — file-based by design, confirmed correct
Logged missing Phase 0 contracts (source manifest, processing profile, review states) as a task
Added task: review Live session round types to support architectural decision capture

## Next Steps
Resolve Live session #011
Install and smoke-test PySceneDetect against a sample video from the archive
Write missing Phase 0 contract documents: source manifest, processing profile, review states
Design and build spine ingestion pipeline script (cis_spine_intake.py)
Write ADR-033 covering segmentation policy: scene detection + duration fallback + transcript-assisted option
Begin cis_reorientation.md update to reflect Phase 1 architecture and new canonical objects

## Notes
ADR-031 needs amendment to include segmentation policy thresholds — deferred to ADR-033
PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True still needs to be added permanently to gpu-test activation script
Next ADR number is ADR-033
Missing Phase 0 contracts are not blocking Phase 1 build but must be written before Phase 1 exits
LIFE domains (Home, Body, Mind) are designed in ADR-032 but deferred until CREATION pipeline is proven
The 2_CIS_REORIENTATION.md still reflects Phase 0 architecture — update required at next session open

## Sync Status
✓ Session logged to database
✓ ADRs.md regenerated (33 decisions)
✓ Knowledge records synced
✓ Projects folder synced
✓ Runtime scripts synced to vault
✓ system_log.md updated

## How to start next session
Run on Ubuntu VM:
```
cis-start
```
Paste output as first message in new chat, then attach this file.
