# CIS Session Handoff
## Date: 2026-04-23
## Time: 0139 UTC

## Claude's Duty at Session Start

At the start of every session Claude must:

1. Read the handoff file and 2_CIS_REORIENTATION.md
2. Confirm the current build target from Next Steps
3. Instruct the human to open a CIS Live session before any build work begins:
   - Topic = current build target
   - Problem = where we are, what we are about to build, and the expected
     return point to the main path if this is a branch
4. Keep that Live session referenced throughout — log unexpected insights
   or architectural decisions as rounds
5. Remind the human to resolve the Live session before closing

Claude must not begin build work until the Live session is open.

---

## Claude's Duty at Session End

At the end of every session — when the human signals the session is ending —
Claude must:

1. Confirm the CIS Live session has been resolved (or note it as open in Notes)
2. Generate the four session close fields for pasting into the dashboard

Claude generates these fields once, at the exact moment the session ends.
If additional work happens after fields are generated, Claude must say:
"Those fields are now outdated — here are the updated ones:" and regenerate
before anything is pasted.

Field 1 — Session Focus: One sentence. The primary objective of this session.
Field 2 — Completed: Specific past-tense list of what was built, fixed, or decided.
Field 3 — Next Steps: Prioritized list of what the next session does first.
Field 4 — Notes: Decisions discussed but not locked. Warnings or gotchas. Mid-thought or
partially built work. Anything deferred that is not a next step. Omit if nothing qualifies.

---

## Session Focus
Reoriented to build roadmap, resolved Live session hygiene, documented WIAS workbook as foundational reference, and installed video processing dependencies.

## Completed
Fixed Resolve button — form was never built, added solution textarea and confirm button
Resolved all open Live sessions (#001–#008)
Located Qwen2.5-VL-32B weights (64GB at confirmed path) and updated registry
Registered Qwen2.5-VL-7B (16GB, confirmed present) as new local model
Confirmed ensure_tables wiring is correct by design — no change needed
Logged ADR-030: WIAS Project Manager as foundational design reference
Added WIAS workbook to Claude project files
Mapped current build state against canonical roadmap — at Phase 0 → Phase 1 transition gate
Confirmed video tutorial indexing is a core use case, not deferrable
Installed ffmpeg and openai-whisper for video preprocessing

## Next Steps
mplement video preprocessing in cis_preprocess.py (ffmpeg frame sampling + whisper transcription)
Define timestamp-linked source_unit schema for video knowledge records
Select 10 files from archive spanning PDFs, images, and at least one video for Phase 0 exit run
Run full pipeline on selected files and produce 10–20 draft knowledge records
Review records and confirm Phase 0 exit criteria met

## Notes
Next ADR number is ADR-031
Whisper model size not yet decided — medium recommended for quality/speed balance on CPU
Video records need segment-level source_unit (e.g. segment_004 with timestamp_start/end fields) — this is a schema addition before the archive run
Archive top-level structure already maps to WIAS domains — intake classification has strong folder-structure signal to leverage
Chat logs from build process should NOT be ingested — signal already captured in ADRs and handoff files
ADR panel sort/filter task logged in dashboard

## Sync Status
✓ Session logged to database
✓ ADRs.md regenerated (31 decisions)
✓ Knowledge records synced
✓ Projects folder synced
✓ Runtime scripts synced to vault
✓ system_log.md updated

## How to start next session
Run on Ubuntu VM:
```
cis-start
```
Paste output as first message in new chat, then attach this file.
