# CIS Session Handoff
## Date: 2026-04-27
## Time: 0710 UTC

## Claude's Duty at Session Start

At the start of every session Claude must:

1. Read the handoff file and 2_CIS_REORIENTATION.md
2. Confirm the current build target from Next Steps
3. Instruct the human to open a CIS Live session before any build work begins:
   - Topic = current build target
   - Problem = where we are, what we are about to build, and the expected
     return point to the main path if this is a branch
4. Keep that Live session referenced throughout — log unexpected insights
   or architectural decisions as rounds
5. Remind the human to resolve the Live session before closing

Claude must not begin build work until the Live session is open.

---

## Claude's Duty at Session End

At the end of every session — when the human signals the session is ending —
Claude must:

1. Confirm the CIS Live session has been resolved (or note it as open in Notes)
2. Generate the four session close fields for pasting into the dashboard

Claude generates these fields once, at the exact moment the session ends.
If additional work happens after fields are generated, Claude must say:
"Those fields are now outdated — here are the updated ones:" and regenerate
before anything is pasted.

Field 1 — Session Focus: One sentence. The primary objective of this session.
Field 2 — Completed: Specific past-tense list of what was built, fixed, or decided.
Field 3 — Next Steps: Prioritized list of what the next session does first.
Field 4 — Notes: Decisions discussed but not locked. Warnings or gotchas. Mid-thought or
partially built work. Anything deferred that is not a next step. Omit if nothing qualifies.

---

## Session Focus
Session Focus: Deploy the verification layer infrastructure (cis_verify.py v12, ADR-041 migration, ADR-040/041/042 locked) and provision local model storage (second 250GB SSD added, Slot 1 Qwen3-32B-AWQ downloaded and operationally verified).

## Completed
Gemini adversarial re-audit of v11 package — CONDITIONAL PASS, three fixes identified
Produced cis_verify_v12.py — three Gemini fixes applied (atomic registration, live dependency hashing, strict query typing), syntax clean, 1733 lines
Gemini re-audit of v12 — PASS, two-auditor requirement satisfied
cis_migrate_041.py run on VM — PASS (17/17), manifests table and live_rounds columns created
cis_verify.py v12 self-check on VM — PASS (41/41)
ADR-040 and ADR-041 logged to DB via dashboard
ADR-042 locked — runtime split: vLLM for reasoning/verification, transformers+bnb for vision extraction
Created /mnt/projects/cis/docs/ADRs/ directory
Second 250GB WDC SSD (sda, serial 172533803130) passed through to VM as virtio6 → vdf
vdf2 formatted ext4, mounted as /mnt/models2 (228GB), added to fstab
Qwen3-32B-AWQ downloaded to /mnt/models (19GB, 4 shards complete)
Slot 1 operational test — PASS (awq_marlin kernel, enforce_eager=True)
2_CIS_REORIENTATION.md updated to reflect all session changes

## Next Steps
Place updated 2_CIS_REORIENTATION.md on VM at /mnt/projects/cis/handoff/
Download Slot 2: DeepSeek-R1-0528 or DeepSeek-R1-Distill-Qwen-32B (verify availability and size first)
Download Slot 3: Qwen3-30B-A3B MoE
Write vLLM startup script (serve Slot 1 as OpenAI-compatible API endpoint on localhost)
Register downloaded models in models table (ADR-024/025)
Wire Intel sidebar LOCAL tab to live model data
Resume Phase 1 pipeline: run cis_classify.py text__all_insights__001

## Notes
vllm-env shebang points to wrong path — always use /mnt/models/vllm-env/bin/python explicitly
Slot 1 production flags: quantization=awq_marlin, enforce_eager=True, max_model_len=4096, gpu_memory_utilization=0.90
KV cache at 4096 context = 1.43GB / 5840 tokens — adequate for reasoning tasks, monitor if longer contexts needed
ADR-041 markdown file placed in /mnt/projects/cis/docs/ADRs/ (not canonical — DB is canonical)
CIS Live session #017 used for Gemini v12 verification round — resolve before closing

Field 5 — Verification Status:
Manifests produced: 0 (no manifest-producing build actions this session — all actions were infrastructure/OS level)
Verifications run: 2 (migration verify PASS, self-check PASS)
Unresolved FAILs: 0
Verification log: /mnt/projects/cis/logs/verification_log.md

## Sync Status
✓ Session logged to database
✓ ADRs.md regenerated (43 decisions)
✓ Knowledge records synced
✓ Projects folder synced
✓ Runtime scripts synced to vault
✓ system_log.md updated

## How to start next session
Run on Ubuntu VM:
```
cis-start
```
Paste output as first message in new chat, then attach this file.
