# Resolution — reviewer-measurement

- lineage: advisor
- run tag: reviewer-measurement @ 2026-09-10T16:42:16-05:00
- at: 2026-09-10T16:42:16-05:00
- prompt_tokens: 345392
- completion_tokens: 2982
- total_tokens: 348374

---

RESOLVE: COMPLETE

[FACT] Unresolved 1 (mediated-surface interface contract location / "read-shaped by construction" definition): CHECKED. No parent architecture document defining a reviewer-surface interface contract exists in the repo. The interface contract is defined in code, not in a separate spec: `runtime/mcp_bridge/tools.py` lines 22-41 define `READONLY_TOOL_NAMES` (18 tools, all read-only) and `tools_for_mode(mode)` which filters the list. `runtime/mcp_bridge/server.py` lines 46-57 enforce the allow-list at the call boundary (`if name not in allowed: return isError`). The tools are read-shaped by construction, not by policy: `_scope_path()` (tools.py line 55) only resolves a repo-relative path (never writes), `_run_git()` (line 67[FACT] (continued) assembles the git argument list from hardcoded subcommands (`git log`, `git show`) — the reviewer never supplies a command string, so there is no shell surface. `cis_hash_file` only computes SHA-256 (no write). `cis_read_file` and `cis_search_files` are scoped to repo root with path-escape prevention in `_scope_path`. No generic shell, interpreter, or command-execution tool is in `READONLY_TOOL_NAMES`. The profiles (`enforcement/mwl-proof-v2/profiles/advisor.yaml` and `evaluator.yaml`) both set `CIS_MCP_MODE: readonly`, `platform_toolsets: []`, and 79 skills disabled. The evaluator's reconciliation noted the boundary between "structurally read-shaped" and "policy-restricted shell" — the code shows it is structural: the server only registers tools from `tools_for_mode("readonly")`, and those tools have no write/dispatch/execute capability by their function signatures. The BUILD section's constraint "read-shaped by construction" is enforceable here and now from the code. Both remedies (advisor's scope-internal acknowledgment and evaluator's interface schema reference) are satisfied: the acknowledgment is the `# READ-ONLY BY DESIGN` comment block in both profile YAMLs, and the interface contract is the `READONLY_TOOL_NAMES` set plus `tools_for_mode` plus the server's allow-list enforcement. No separate parent document is needed. RESOLVED: the mediated surface is defined in code at `runtime/mcp_bridge/tools.py` and `runtime/mcp_bridge/server.py`, enforced structurally, and referenced by both profile YAMLs.

[FACT] Unresolved 2 (whether the "item" definition problem is practically material): CHECKED against the actual reconciliation artifact in this very packet. The advisor's reconciliation lists 5 AGREED items, 1 DISAGREE, 4 "THEY MISSED", and 2 "I MISSED". The evaluator's reconciliation lists 4 AGREED, 0 DISAGREE, 1 "THEY MISSED", and 4 "I MISSED". The two lineages produced findings at different granularities — the advisor split into S1/S2/S3/S4/C1-C6, the evaluator used G1/G2/G3 — and the reconciliation mapped them by semantic overlap, not by a canonical item list. The advisor itself noted this: "We are mapping findings by semantic overlap rather than by a canonical item list." The evaluator agreed: "Our own reconciliation right now is living this problem." The seeded-disagreement fixture tests one controlled case; this organic review demonstrates the mapping problem is real, not theoretical. RESOLVED: the "item" definition problem IS practically material, proven by this reconciliation artifact where two lineages produced non-isomorphic finding sets requiring ad-hoc semantic mapping. The card should address this.

[FACT] Unresolved 3 (whether 8649/8650 identifiers are self-evident to the implementer): CHECKED. `tools/advisor_review.sh` line 1 of its header comment states: "send a review packet to the advisor (advisor, 8649)". Line 41: `LINEAGES=("advisor:8649" "evaluator:8650")`. `enforcement/mwl-proof-v2/profiles/advisor.yaml` line `api_server.port: 8649`. `enforcement/mwl-proof-v2/profiles/evaluator.yaml` line `api_server.port: 8650`. The identifiers are Hermes gateway ports, resolvable in the project's configuration context: each port maps to a profile YAML that defines the model, MCP mode, and skill disablement. The card's SOURCE/INTENT section says "advisor (8649) -> GLM lineage" and "evaluator (8650) -> Qwen lineage" — this is sufficient context for an implementer who has read `advisor_review.sh` or either profile YAML. RESOLVED: the identifiers are gateway port numbers, self-evident from `tools/advisor_review.sh` and the two profile YAMLs. No change needed to the card.

[FACT] Advisor UNRESOLVED 2 sub-point (evaluator's: "Evidence to settle this would be a harness-level schema or ontology that forces both lineages to map their findings to a canonical set of evaluation criteria before cross-feed"): CHECKED. No such schema exists in the repo. `tools/advisor_review.sh` has no structured-output schema for reviewer findings — round-1 output is freeform prose written to `reviews/done/<id>.<profile>.response.md`. The `--reconcile` mode (line 49) cross-feeds the verbatim frozen artifact; there is no pre-cross-feed structuring step. The `deliberation_rounds` table (per OQ-SEED-006) does not even have a `reviewer_output` column — only `reviewer_signal`. RESOLVED: no harness-level schema or ontology exists. The evaluator's proposed remedy (canonical criteria set before cross-feed) is not implemented and would require both a schema change to `deliberation_rounds` and a prompt-structuring step in `advisor_review.sh`.

[SPEC] Based on the resolution of Unresolved 2 and the evaluator's sub-point: Add to the BUILD section, after step 4, a new clause: "5. The harness defines a canonical evaluation-criteria checklist derived from the card's DONE WHEN section. Each reviewer maps findings to this checklist before round-1 freeze. Round-2 reconciliation maps item-by-item against this shared checklist, not by ad-hoc semantic overlap. Findings that do not map to any checklist item are labeled UNMAPPED and preserved as dissent." This belongs in the BUILD section between the current step 4 and step 5. This is a SPEC because the card currently says "item-by-item" without defining "item" — the checklist makes the item boundary harness-defined rather than reviewer-defined, closing the granularity-mapping gap that this reconciliation artifact demonstrated is practically material.

[FACT] Advisor UNRESOLVED 1 also asked: "confirmation of whether a parent architecture document exists in the project that defines interface contracts for reviewer surfaces." CHECKED via `cis_search_files` for `reviewer.*surface|mediated.*interface|read-shaped` — the git grep returned an error (empty index or no matches in tracked files). `cis_search_knowledge` returned no results (FTS5 column error and semantic search permission denied). However, the direct file reads confirm the interface contract lives in `runtime/mcp_bridge/tools.py` and `runtime/mcp_bridge/server.py`, not in a separate architecture document. The DEV-PIVOT documents (checked via `cis_get_dev_pivot_status`) cover enforcement architecture (DEV-PIVOT-17) and process isolation but do not define a reviewer-surface interface schema. RESOLVED: no parent architecture document exists for this purpose. The interface contract is the code itself.

[AUTHORITY] Whether the card should require a formal "read-shaped by construction" definition clause to be added to its BUILD section, or whether the existing code-level enforcement (READONLY_TOOL_NAMES + server allow-list + profile YAML) is sufficient as-is. Option A: Add a clause to the BUILD section stating "A tool is read-shaped by construction only if its handler function cannot write, execute, or dispatch — verified by the READONLY_TOOL_NAMES set in runtime/mcp_bridge/tools.py and the server's allow-list enforcement in runtime/mcp_bridge/server.py." Option B: Leave the card as-is, relying on the existing code and the NOT IN THIS CARD deferral to CARD-reviewer-isolation. Recommendation: Option B, because the card's own philosophy is narrow scope and the code already enforces the constraint structurally — adding a code-reference to the card couples the spec to a specific implementation, which the card correctly avoids. The detection limitation (probes cannot detect a general execution surface) is already acknowledged by the NOT IN THIS CARD section. This is the operator's call because it is a scope/threat-model boundary decision: whether this card owes the implementer a code-level reference or whether that belongs in CARD-reviewer-isolation.
