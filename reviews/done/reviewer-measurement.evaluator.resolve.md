# Resolution — reviewer-measurement

- lineage: evaluator
- run tag: reviewer-measurement @ 2026-09-10T16:42:16-05:00
- at: 2026-09-10T16:42:16-05:00
- prompt_tokens: 283188
- completion_tokens: 5036
- total_tokens: 288224

---

RESOLVE: INCOMPLETE

[FACT] UNRESOLVED-advisor-1 / UNRESOLVED-evaluator-1a (mediated-surface contract location): No parent architecture document defining reviewer interface contracts exists in the repo. The interface contract is already defined concretely in two places: (1) `enforcement/mwl-proof-v2/profiles/advisor.yaml` and `evaluator.yaml`, which specify `CIS_MCP_MODE=readonly` on the cis-knowledge MCP bridge, `skills.platform_disabled`, and `platform_toolsets: []`; (2) the MCP bridge server itself (`mcp_bridge.server`), which "serves only the read-shaped subset (spine + file + git + hash) and refuses dispatch at the call boundary." The advisor's remedy is therefore the pragmatic one — there is no parent document to reference — but the card should explicitly point to where the contract IS enforced (the profile YAMLs and MCP mode flag) rather than leaving "mediated" undefined. Cite: `enforcement/mwl-proof-v2/profiles/evaluator.yaml` lines 1-7 and lines 52-62[FACT] UNRESOLVED-evaluator-1b (does the mediated-surface gap belong to this card or to CARD-reviewer-isolation?): The read-shaped-by-construction constraint IS already enforceable at the interface level and IS already enforced. Both profile YAMLs specify `CIS_MCP_MODE=readonly` on the MCP bridge, which "serves only the read-shaped subset (spine + file + git + hash) and refuses dispatch at the call boundary." Combined with `skills.platform_disabled` and `platform_toolsets: []`, the interface structurally prevents a generic shell — this is not a policy constraint, it is an architectural one. The card's BUILD constraint is therefore already satisfied by existing infrastructure. The remaining gap — that the measurement probes cannot independently detect a generic execution surface — is correctly the isolation card's territory, since detection of write capability is a different question than enforcement of read-only access. Cite: `enforcement/mwl-proof-v2/profiles/advisor.yaml` lines 52-62, `enforcement/mwl-proof-v2/profiles/evaluator.yaml` lines 52-62.

[SPEC] UNRESOLVED-advisor-1 / UNRESOLVED-evaluator-1 (resolution): Add a clause to the card's BUILD section: "The mediated reviewer surface is the cis-knowledge MCP bridge in CIS_MCP_MODE=readonly, as configured in the advisor and evaluator profile YAMLs. 'Mediated' means the bridge serves only read-shaped instruments (file read, file search, spine query, git inspection, hash comparison) and refuses all other tool calls at the call boundary. This is an architectural constraint, not a policy file." This replaces the undefined "mediated" with a pointer to the existing enforcement mechanism. Location: BUILD section, immediately after the bullet list of five instruments.

[FACT] UNRESOLVED-advisor-2 / UNRESOLVED-evaluator-2 (whether the "item" definition problem is practically material): The reconciled review itself is a live data point. Both lineages produced findings at different granularities and framings (advisor produced numbered S/C findings, evaluator produced G findings), yet mapped them by semantic overlap successfully — identifying 4 agreements, 1 disagreement, and distinct "they missed / I missed" items without ambiguity. The seeded-disagreement fixture tests a controlled case, but this organic reconciliation demonstrates the mapping works in practice. The concern is theoretically valid but not practically material given the current reviewer lineages' output structure. No card change required. Cite: the reconciled review above, advisor AGREED items 1-5 mapping to evaluator AGREED items 1-4.

[AUTHORITY] UNRESOLVED-advisor-3 / UNRESOLVED-evaluator-3 (whether 8649/8650 are self-evident): The identifiers ARE self-evident to the implementer. They are the API server ports for the two reviewer profiles: 8649 = advisor (GLM, `z-ai/glm-5.2`), 8650 = evaluator (Qwen, `qwen/qwen3.7-max`). Documented in: `enforcement/mwl-proof-v2/entrypoint.sh:47` ("advisor (8649) and evaluator (8650) added 2026-09-03"), `enforcement/mwl-proof-v2/profiles/advisor.yaml:45` (port: 8649), `enforcement/mwl-proof-v2/profiles/evaluator.yaml:41` (port: 8650), `tools/advisor_review.sh:136` (`LINEAGES=("advisor:8649" "evaluator:8650")`), and `docs/PHASE_2_CANDIDATE_ITEMS.md:15` ("GLM `advisor` 8649 / Qwen `evaluator` 8650"). The card should still add a brief inline parenthetical for reader convenience: "(8649) and (8650) are the Hermes API-server ports for the advisor and evaluator profiles respectively." No operator decision needed — this is a documentation convenience, not a scope question.

End of resolution.
